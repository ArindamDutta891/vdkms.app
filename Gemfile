source "https://rubygems.org"

gem "webrick", "~> 1.7"  # WEBrick server for serving Jekyll locally
gem "jekyll", "~> 4.3"  # Jekyll static site generator
gem "jekyll-environment-variables"  # Plugin for setting environment variables in Jekyll
gem 'csv', '3.3.0'

gem 'base64'  # Base64 encoding and decoding library
gem 'google-protobuf'  # Google's protocol buffers library
gem 'jekyll-scholar'  # Jekyll plugin for managing bibliographies and citations
#gem 'wdm', '>= 0.1.0'
gem 'rexml', '3.3.2'

# Specify gems for Windows platforms
platforms :mingw do
  gem 'wdm', '>= 0.1.0'  # Windows-specific gem for file change detection optimization
end
